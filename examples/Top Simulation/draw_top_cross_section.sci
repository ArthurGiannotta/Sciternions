function draw_top_cross_section(S)
    //  Arthur Clemente Giannotta ;

    // call this function and see that there are no axes, fix so that the axes are drawn so we get a notion of size
    xfpolys(S.crossX, S.crossZ, color("brown"))
endfunction
